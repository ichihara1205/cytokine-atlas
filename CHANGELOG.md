# CHANGELOG

データを変更したら1行足す（日付・分子・何を変えたか・出典）。
「いつ誰が何を根拠に書いたか」が追えなくなった時点でこのサイトは信用を失う（CLAUDE.md §0.4）。

## 2026-08-31

### 類洞クロストーク総説から産生・受容を出典付け（verified 102 → 130）

出典: Gibert-Ramos A, et al. Sinusoidal communication in chronic liver disease.
Clin Mol Hepatol 2025;31:32-55. **PMID:39355871** / doi:10.3350/cmh.2024.0734
（オープンアクセス CC BY-NC 3.0。PubMed 経由で参照）

**図を読む前に本文を読んだところ、狙っていた辺のほとんどが本文に文章で書かれていた。**
図読みが必要だったのは結局2件だけ。以下は本文の記述に基づくので `verified`。

| 分子 | 辺 | 本文の記述 |
|---|---|---|
| GDF2 (BMP-9) | HSC → LSEC | HSC が放出する BMP-9 が LSEC の健常な表現型の維持を促す |
| VEGFA | HSC・肝細胞 → LSEC | VEGF は HSC と肝細胞が分泌し、LSEC の健常な表現型を保つ |
| HGF | LSEC → 肝細胞・HSC | LSEC が Wnt2a と HGF を分泌し、HSC を静止状態に保ち肝細胞に再生を促す |
| PDGFB | LSEC → HSC | 毛細血管化した LSEC が PDGF・TGF-β の分泌で HSC を能動的に活性化する |
| TGFB1 | LSEC → HSC・KC | LSEC の TGF-β ファミリーリガンドが KC の分化・表現型維持に寄与し、傷害時は HSC を活性化する |
| TNF | KC → 肝細胞 | KC が分泌する TNF-α が肝細胞の NF-κB を活性化する |
| IL1B / IL18 | KC → HSC | KC のインフラマソームがカスパーゼ1を活性化して成熟させ、両者が HSC の活性化に直接寄与する |
| IL1A | KC（産生） | KC における IL-1α/β の分泌が傍分泌的に線維化を促す |
| IL10 | LSEC・KC（産生） | LSEC と KC の IL-10 分泌が肝の免疫寛容の鍵になる |
| HAMP | 肝細胞（産生） | ヘプシジンは主に肝細胞が産生し、LSEC が鉄を感知して分泌する BMP リガンドが部分的に制御する |

**BMP6 には何も付けていない。** 原著は「BMP ligands」としか書いておらず、どの BMP かを
特定していない。分子を1つに絞れない記述から特定の分子の辺を作らない。

いずれの note にも「出典はこの細胞種の生体内での性質を述べたもの。使っている株・iPS由来細胞が
それを再現するかは別」を付けた。TMNK-1 や LX-2 の実測ではない。

### 図から読み取り（`figure_read` の初適用、2件）

Fig. 4C（ヒト肝の単一細胞データにおける経路別 relative information flow を健常/肝硬変で比較した棒グラフ）。
**この図の内容は本文に一切書かれていない**（TWEAK は本文全文検索で0件）。

- **TNFSF12 (TWEAK)** `effects` — 棒は肝硬変が 1.0 を占め健常の区画が視認できない。肝硬変にほぼ限局。
- **SPP1** `effects` — 肝硬変がおよそ 3/4。健常肝でも動くが肝硬変で大きく優位。

いずれも棒の高さは目視であり、数値は論文に記載がない。その旨を `read_note` に書いて
`/todo` に常時表示している。

### 重複キーのバグ（データが黙って消えていた）

`producers` / `receivers` に同じ `cell_id` が2回書かれている分子が5件あった
（CX3CL1, CXCL12, HGF, PDGFB, VEGFA）。**yaml は重複キーを後勝ちで黙って捨てるため、
パース後のデータからは検出できない。** 実際 VEGFA では今回付けた出典が前側の行に当たり、
そのまま捨てられていた。

- `scripts/validate.py`: ローダを `DupKeyLoader` に差し替え、全ファイル・全階層の重複キーを
  行番号付きで ERROR にした。パース前に構文木を見るのでこれ以外に検出手段がない。
- 既存5件は level は高いほう、note は残るほうを採ってマージ。

### 表示

- `scripts/build.py`: ビルド出力の集計に `図から読取` を追加（それまで数が出ていなかった）。
- `/todo` の「図から読み取ったもの」表の「内容」列が `note` を出していたのを `read_note`
  （図から何をどう読んだか）に変更。図読みの検算に必要なのはこちら。

## 2026-08-11

- **scripts/validate.py / scripts/build.py を新規作成**（フェーズ1）。
- **scripts/fetch_hgnc.py 追加**。HGNC 公式の全遺伝子セットから `data/hgnc/symbols.tsv` を生成
  （承認 45,031 シンボル＋旧シンボル・別名）。gene の照合は推測ではなくこの実データで行う。
  出典: HGNC `hgnc_complete_set.txt`（2026-08-11 取得）
- **config.yaml 新規作成**。`include_own_data: false`、系内細胞の定義、5×5行列の軸。
- **data/cells/registry.yaml 新規作成**。CLAUDE.md §4 の14細胞を登録。
  分化辺は `differentiation.from` のみ。プロトコルとマーカーは出典が無いため `status: todo`。
- **data/families/{structure,receptor,naming}.yaml 新規作成**。3軸で計21ファミリー。
  出典が取れた2件のみ `verified`、残りは `inferred`。
- **`_TEMPLATE.yaml` をルートから `data/molecules/` へ移動**し、`form` と `effects` に
  `source` を追加（verified なのに source が無く、validate.py が止めていたため）。

### 出典の訂正（重要）

CLAUDE.md と旧 `_TEMPLATE.yaml` に書かれていた PMID を PubMed で照合したところ、
4件中3件が別の論文を指していた。実在確認のうえ差し替えた。

| 記載箇所 | 旧PMID | 旧PMIDの実際の中身 | 正しいPMID |
|---|---|---|---|
| IL-6 肝（Schmidt-Arras & Rose-John, J Hepatol 2016） | 27085250 | 頸部・腰痛の教育効果メタ解析 | **26867490** |
| GDF15 受容体 GFRAL+RET | 28965740 | 潜在性甲状腺機能低下症 | **28846099**（Yang L, Nat Med 2017） |
| iKC 分化（Tasnim et al. 2019） | 30690240 | バイオ炭と窒素動態 | **30497023**（Biomaterials 2019） |
| TGF-βスーパーファミリー | 27141051 | — | 27141051（Morikawa M, CSH Perspect Biol 2016。**一致していた**） |

30497023 は `data/cells/registry.yaml` にはまだ入れていない（iKC の分化プロトコル欄は
`status: todo` のまま）。原著を確認してから記入する。
CLAUDE.md 本文中の PMID は未修正なので、参照するときは上表を優先する。

## 2026-08-11（続き）— フェーズ1の分子群

- **分子56件を新規作成**（`data/molecules/*.yaml`）。肝共培養系＋iPS分化に関係するものを選定。
  gp130群4／IL-1群5／γc群4／クラスII 5／IL-12・IL-17 3／TNF-SF 3／TGF-β-SF 6／
  ケモカイン7／増殖因子8／CSF群4／マトリセルラー3／アディポカイン2／肝特異的2。
- **ファミリーを21→57件に拡張**。構造21／受容体27／慣用名9。「名前と実体のズレ」は45件登録。
- 遺伝子シンボルは全件 HGNC 承認シンボルで照合済み。`CTGF` は旧シンボルのため `CCN2` を採用。
  `M-CSF(CSF1)` の構造軸は当初システインノットとしたが4ヘリックスバンドルが正しいので訂正した。

### status の内訳

`verified 25 / inferred 741 / todo 4`。
verified は、PubMed で実在とタイトルを確認済みの3文献
（PMID:26867490 / 27141051 / 28846099）が支える記述に限られる。**それ以外は全て `inferred`＝未検証**で、
産生・受容の level、effects の符号、受容体構成はいずれも一般知識から書いたものである。
出典を当てて `verified` に上げる作業は `/todo` ページを作業リストとして進める。

### system_closure の自動判定の結果

`closed 38 / out_only 9 / in_only 8 / external 1`。

- **out_only（系内に受け手がいない）**: CXCL8, CXCL9, CXCL10, G-CSF, IL-15, IL-12p40, IL-23p19, FGF-21, GDF-15
- **in_only（系内に出し手がいない）**: IFN-γ, IL-4, IL-17A, IL-21, IL-22, FGF-19, レプチン, アディポネクチン
- **external**: IL-2

GDF-15 は受け手が系外の登録細胞にも存在しない（受容体 GFRAL が脳幹限定）。
FGF-19 は産生元が回腸、レプチン／アディポネクチンは脂肪細胞と、いずれも系外にしか出し手がいない。

## 2026-08-11（続き2）— 表示と細胞スキーマの見直し

- **細胞の `kind` を廃止し、`stage` と `origin` に分割**（スキーマ変更。3ファイルに影響）。
  `kind: mature | progenitor | line` は「分化段階」と「由来」という無関係な2軸を
  1フィールドに潰していた。ファミリーを3軸に分けたのと同じ理由で分離する。
  - `stage:  pluripotent | progenitor | mature`
  - `origin: primary | line | ipsc_derived | null`（null＝実際には培養していない参照ノード）
  LX-2 は mature かつ line、iKC は mature かつ ipsc_derived、qHSC は mature かつ primary。
  validate.py は `kind` が残っていたらエラーで止める。
- **`label_generic` を追加**。図・一覧の主表示を総称（Hepatocyte / HSC / LSEC / KC / Mφ）にし、
  由来と実体を必ず併記する（「HSC ／ 株 / LX-2 / 活性化」）。
  同じ HSC でも株か初代かでデータの読み方が変わるため、総称だけの表示にはしない。
- **分子一覧をカードからリスト（テーブル）に変更**。列見出しクリックで並び替え（昇順→降順→元の順）。
  絞り込みと併用できる。
- **効果を色分け**（負＝青 / ±0＝灰 / 正＝赤、濃さが絶対値）。
- 既存 CSS の `.eff+1` `.b+0` などは `+` を未エスケープで書いており、そもそも適用されていなかった。
  `vm2/vm1/v0/vp1/vp2` に置き換えて修正。
- `.effv` に `display:inline-block` を付けたまま `<td>` に当てていたため、
  該当セルがテーブルレイアウトから外れて列がずれていた。バッジを `<span>` に入れて修正。

## 2026-08-11（続き3）— 見出し固定と色の対応表

- **分子一覧の列見出しをスクロール固定**。表自体を `max-height:70vh` のスクロール領域にした。
  `position:sticky` は「縦スクロールが起きている親」の中でしか効かないため、
  従来のように `overflow-x:auto` のラッパーに入れたままページ側でスクロールすると固定されない。
- **分子名の列を横スクロール時に左端へ固定**（列が多く横に流れるため）。
- **色と細胞の対応表（凡例）を追加**。産生・受容のドットが何色かを表の中で見失わないようにする。
  凡例はスクロール領域の内側に置き、見出しと積み重ねて上端に固定する。
  領域の外に置くと、画面の低いノートPC（800px）ではページを送ったときに凡例だけ視界から消える
  ことを実測で確認したため。凡例の高さは折り返しで変わるので JS で実測して
  `--legend-h` に入れ、見出しの固定位置をその分だけ下げている。

## 2026-08-11（続き4）— 記載の偏りの可視化と用語の平易化

### 細胞ごとの記載件数が大きく偏っていることを明示

producers / receivers の記載件数を数えたところ、`config.yaml` の `matrix.cells` に挙げた
5細胞に集中していた。株か初代かiPS由来かは関係なく、**執筆時にその5細胞を既定セットとして
埋めたことによる作業上の偏り**である。

    Hepatocyte/PXB 初代 60 ／ HSC/LX-2 株 54 ／ KC/iKC iPS由来 50
    LSEC/TMNK-1 株 40 ／ Mφ/THP-1 株 31
    ── 以下は行列外 ──
    Hepatoblast 7 ／ preMac 6 ／ LSEC/iLSEC 4 ／ iPSC 4 ／ HSC/qHSC 2 ／ KC/pKC 0

件数の少なさは通信の少なさではなく未記載を意味する。これが「なし」に見えると
system_closure の判定（このサイトの中心的価値）を誤らせるため、以下を追加した。

- `/todo` に**細胞ごとの記載状況**の表を追加。記載が特に遅れている細胞を色付きで示す。
- 細胞ページで産生・受容が0件のとき、「登録なし」ではなく
  **「まだ1件も記載していない。出さない／受け取らないという意味ではない」**と明示する。

### 用語の平易化

`通信辺` `分化辺` を説明なしにラベルとして使っていたので、意味を併記した。

- 通信辺 → 「どの細胞が出して、どの細胞が受け取るか」＝別々の細胞の間の同時点の会話
- 分化辺 → 「この細胞が何から変わり、何に変わるか」＝同じ細胞の時間の流れ

矢印の意味が違うので同じ図に混ぜない、という §4 の指示の理由が読んで分かる形にした。

## 2026-08-11（続き5）— 記載の穴埋めと出典付け

### Step 1: 「未評価」と「調べた上でなし」を区別できるようにした（コード）

新しいフィールドは足さず、既存の `level: 0`（CLAUDE.md §5 の「なし」）から導出する。

- **評価済み** = その細胞が `producers`/`receivers` にキーとして現れる（level 0 を含む）
- **未評価** = キーが無い

`scripts/build.py`:
- `cell_coverage()` に `assessed` / `unassessed` を追加。`/todo` の記載状況表を
  「評価済み / 産生 / 受容 / 未評価」に変更（分母は分子数）
- `render_cell()` が level 0 を拾い、「評価したうえで『なし』と判断したもの」として別に表示。
  従来は `level >= 1` で落としていたため、未記載と区別がつかなかった

### Step 2: 出典付け（verified 25 → 63）

**基準: abstract がその主張に出てくる具体名を挙げているものだけ verified にする。**
総説の abstract は「ケモカインは GPCR を使う」とは書くが「CCL2 の受容体は CCR2」とは書かないため、
ファミリー単位の主張のみ verified にし、分子ごとの受容体対応は inferred のまま残した。

新規に採用した文献（全て PubMed で実在とタイトルを確認済み）:

| PMID | 文献 | 裏付けた主張 |
|---|---|---|
| 30995501 | Murakami, Immunity 2019 | gp130 を共有する10メンバーの IL-6 ファミリー |
| 24332029 | Garlanda, Immunity 2013 | IL-1 ファミリーの構成、IL-1Ra が受容体拮抗剤、デコイ受容体 |
| 30995502 | Leonard, Immunity 2019 | γc は IL-2/4/7/9/15/21 受容体の構成鎖。X連鎖SCID。JAK3 |
| 26200010 | Lazear, Immunity 2015 | III型IFN = IL-28/IL-29、上皮優位に作用 |
| 42213608 | Novotny, J Innate Immun 2026 | IFN-λ は IFNLR1 + IL10RB ヘテロ二量体、JAK1/TYK2 |
| 29637711 | Hughes & Nibbs, FEBS J 2018 | ケモカインは7回膜貫通GPCRを介する |
| 30354964 | Dostert, Physiol Rev 2019 | TNF/TNFR-SF は19リガンド29受容体 |
| 24890514 | Stanley & Chitu, CSH 2014 | **CSF1R は CSF-1 と IL-34 の両方で活性化される** |
| 30455427 | Kuro-o, Nat Rev Nephrol 2019 | **α/βKlotho は FGF19/21/23 の高親和性結合に必須。FGF19は腸→肝FGFR4、FGF21は肝→脂肪FGFR1c** |

CSF1R と Klotho の2本は abstract が主張そのものを述べており、
`csf1r-shared` と `fgfr-klotho` は defining_feature・mismatch・分子の receptor まで verified にできた。

**inferred のまま残したもの（意図的）**: producers/receivers 全件、effects 全件、
gp130群・IL-1群・ケモカイン各分子の個別の受容体構成。原著の図か、より具体的な総説が要る。

### Step 3: 対比ペアの穴埋め（inferred +56件）

「モデル細胞 ↔ 基準細胞」の3組に絞り、**差の理由を note に書ける分子だけ**追加した。

| 細胞 | 評価済み（前 → 後） |
|---|---|
| KC 初代 / pKC | 0/56 → 14/56 |
| LSEC iPS由来 / iLSEC | 4/56 → 12/56 |
| HSC 初代 / qHSC | 2/56 → 10/56 |

主な内容:
- **pKC**: CSF1R・CX3CR1・フェロポルチンといったクッパー細胞の定義的性質を受け手側に置き、
  iKC の成熟度を測る基準として使えるようにした
- **qHSC**: HGF は静止期で高く活性化で落ちる（LX-2 では過小評価）、PDGFRB は活性化で誘導される
  （LX-2 では過大評価）、CCN2/TIMP1 は活性化マーカーなので静止期では低い、という対比を記載
- **iLSEC**: VEGF-A 受容が有窓構造の維持に必須という軸を明示

加えて **level 0（調べた上でなし）を20件** 明示した（GDF-15 の受容体 GFRAL は脳幹限定、
FGF19 の産生元は回腸、IL-2 は T 細胞限定）。これで該当細胞が「未評価」から「評価済み」に移る。

**system_closure の分類は変化なし**（closed 38 / out_only 9 / in_only 8 / external 1）。
追加した辺はいずれも既に系内の相手がいる分子で、level 0 は辺を作らないため。

## 2026-08-11（続き6）— フェーズ2: 経路図と分子間の関係

フェーズ1の完了条件を満たしたので着手。「どの分子がどの分子と関わるか」「その分子が経路の
どこにいるか」「その経路が何をするか」の3つに答えるための層。

### 第3のグラフとして「分子間辺」を定義

通信辺（細胞↔細胞）・分化辺（細胞→細胞）に続く3本目。混ぜずに別扱いする（§4 と同じ原則）。
語彙は閉じた集合にして validate.py で検証する:
`induces / antagonizes / sequesters / heterodimer_with / synergizes_with / opposes`

**手で書かず導出するもの**（`system_closure` と同じ理由）:
- `shares_receptor_with` — 受容体軸のファミリー所属から計算。手書きは validate.py がエラーにする
- 逆向きの辺（「〜によって誘導される」）— 正方向から生成
- 分子側の `pathways` — `data/pathways/` の `steps[].molecules` から導出。手書きはエラー

### 経路8本を新規作成（すべて `data/pathways/`）

gp130-jak-stat3 / il1r-myd88-nfkb / tgfb-smad / chemokine-gpcr /
tnfr-nfkb-death / class2-jak-stat / gamma-c-jak3 / rtk-pi3k-erk

各経路は `steps`（ligand → receptor → transducer → tf → output）と `outcome` を持つ。
**SVG はデータから生成**しており手描きしていない（§2「図はすべて自作 SVG」）。
既に確認済みの文献で裏が取れた8本は `verified`。

分子56件のうち **13件が経路未割当**（アディポネクチン・CCN2・G-CSF・GDF-15・ヘプシジン・
IL-12p40・IL-17A・IL-23p19・リポカリン2・レプチン・オステオポンチン・TIMP-1・GM-CSF）。
これも「経路が無い」ではなく「書いていない」として一覧に出す。

### 実装中に見つけて直した誤り（2件とも図・データが事実と逆になるもの）

1. **分岐の矢印が交差していた。** 総当たりで結線していたため、SMAD2/3 → 鉄代謝、
   SMAD1/5/8 → 線維化 と読める図になっていた。枝が揃う段は同じ枝どうしのみ結ぶよう修正。
2. **誘導辺が過剰生成されていた。** リガンド×出力の総当たりにしたため、
   `IL-1Ra → IL-6 を誘導する`（拮抗剤なのに誘導）、`TGF-β1 → ヘプシジン`（枝違い）などが
   出ていた。枝を跨がせず、`antagonist: true` のステップを導出元から除くよう修正。
   これで 79 → 62 件に減った。

導出した誘導辺の粒度は「その経路のリガンドなら誘導しうる」までであり、
リガンド1つずつを個別に確かめた結果ではない。その旨を `/relations` に明記した。

### 追加したページ

`/pathway/`（一覧＋経路未割当）、`/pathway/<id>/`（SVG＋各段階＋outcome）、
`/relations/`（分子間辺の一覧・種類で絞り込み・受容体共有の導出）。
分子ページに「経路の中での位置」「他の分子との関係」節、
ファミリーページに対応する経路へのリンクを追加。

## 2026-08-11（続き7）— 経路図を細胞の解剖図に描き直し

「箱と矢印の羅列では論文の図に視覚的に劣る」との指摘を受けて描画を作り直した。
足りなかったのは情報量ではなく**空間の情報**——細胞外・膜・細胞質・核のどこで起きているか。

既存の `kind` 語彙がそのまま区画に対応するため、**データは一切変えずに描画だけを変更**した。

    ligand → 細胞外 ／ receptor → 膜を貫通 ／ transducer → 細胞質
    tf → 核膜にまたがる ／ output → 核内

- 脂質二重層（頭部の丸を並べた2本線）を描き、受容体の箱の幅を制限して
  **膜が左右に見える**ようにした。全幅だと膜が隠れて「貫通している」ことが読めない
- 転写因子の箱を核膜の境界にまたがせた。細胞質で活性化してから核へ入る順序が
  箱の位置そのもので読める
- **矢印にラベルを付けた**（結合する / 受容体が活性化する / リン酸化して核へ移行 /
  標的遺伝子の転写）。無ラベルの矢印は「なんか関係ある」しか伝えない
- `<figure>` + `<figcaption>` + `role="img"` + `aria-label` を付与
- 図のラベルを短くし（30箇所）、説明文は `note` と「各段階」の表に移した

### 修正した誤り

**核を転写因子のある経路にだけ描くようにした。** 当初は出力があれば核を描いていたため、
ケモカイン経路（出力は遊走であって転写ではない）にも核が描かれ、図が事実と食い違っていた。
現在 核あり6本 / 核なし2本（chemokine-gpcr, tnfr-nfkb-death）。

### 既知の制限

TNFR は複合体I（NF-κB→転写）と複合体II（細胞死）に分岐し、片方だけが核へ向かうが、
現在の描画は核を全幅の帯として扱うため「枝ごとに核へ入る/入らない」を表現できない。
そのため TNFR には核を描いていない。誤りではないが不完全。

## 2026-08-11（続き8）— 類洞の断面図と、分子の形

### 1. 類洞の断面図（`sinusoid_svg`）

肝類洞の実際の配置——肝細胞索 / ディッセ腔 / 有窓内皮 / 類洞内腔——を描き、
細胞をその位置に置いた。破線の内皮が fenestrae、星形が星細胞、不定形が貪食細胞。
トップ（行列の前）・分子ページ・細胞ページに載せた。

**分子ページでは 産生細胞 → 分子 → 受容細胞 のハブ型**にした。
最初は産生と受容を総当たりで結んだが、IL-6 で24本になり毛玉になったため、
分子自身を内腔の中央に置く形に変更。辺が (産生数 × 受容数) から (産生数 + 受容数) に落ちる
（IL-6: 24本 → 10本）。青＝産生側から分子へ、赤＝分子から受容側へ、線の太さが level。

`system_closure` が図で読めるようになった。レプチンなら系外の脂肪細胞から1本だけ入って
肝の細胞へ広がる形になり、「受けるだけ」が一目で分かる。

**分化段階のノード（iPSC・前駆マクロファージ・肝芽細胞）はこの図に含めない。**
通信辺の図であり、分化辺と混ぜないため（§4）。図の注記にも明記した。

脂肪細胞は当初 肝細胞索の帯に置いていたが、脂肪組織は肝の外なので位置として誤り。
「系外の組織（血流の先）」の帯を下に足してそこへ移した。

### 2. 分子の形（会合状態）

`form.oligomer` を新設（`monomer / homodimer / homotrimer / heterodimer / multimer`）。
validate.py で語彙を検証し、未設定は WARN に出す。

56分子すべてに設定した。うち20分子は単量体でない:

- ホモ三量体: TNF-α, TRAIL, TWEAK
- ホモ二量体: TGF-β1/2, BMP-2/6, アクチビンA, GDF-15, PDGF-B, VEGF-A, M-CSF, IL-34,
  IL-10, IL-17A, IFN-γ
- ヘテロ二量体: IL-12p40, IL-23p19, HGF（α鎖/β鎖の二本鎖）
- 多量体: アディポネクチン

`CSF1` と `IL34` のみ verified（PMID:24890514 の abstract が "homodimeric growth factors" と明記）。
残りは inferred。

形は円の並びで描き、分子ページの見出し・経路図のリガンド段・類洞図の中央ハブで共通に使う。
四角い箱では二量体と三量体の違いが出ないため。

## 2026-08-11（続き9）— 受容体を膜での形で描き分け

経路図の受容体を一律の箱で描いていたため、gp130 が2本鎖であることも、GPCR が膜を7回貫くことも、
RTK が細胞内にキナーゼドメインを持つことも図から消えていた。

`steps[].architecture` を新設（`receptor` のステップにのみ付く。validate.py が語彙と
付け場所を検証し、未設定は WARN）。

| architecture | 図での描き方 | 経路 |
|---|---|---|
| `class1_shared` | 共通鎖（濃）＋固有鎖（淡）の2本＋会合する JAK | gp130 / γc |
| `class2` | 同上 | クラスII |
| `gpcr` | 膜を7回貫く柱と連結ループ＋Gi | ケモカイン |
| `rtk` | 2本鎖＋細胞内キナーゼドメイン×2（二量体化） | RTK |
| `stk` | II型・I型の2本鎖＋それぞれのキナーゼ | TGF-β |
| `tir` | 細胞外のIg様ドメイン＋細胞内 TIR ドメイン | IL-1R |
| `tnfr` | 3本の鎖（三量体受容体） | TNFR |
| `sefir` | 1本鎖＋SEFIRドメイン | （未使用） |

受容体の段は「箱」をやめ、膜を貫く形そのものを描いてラベルを下に置く形にした。
ステップ名と形の名前が同じ場合は二度書かない。

これで経路図が持つ情報が揃った:
リガンドの会合状態（円の並び）→ 受容体の膜での形 → 細胞質 → 核 → 出力。

## 2026-08-11（続き10）— 受容体の形を「なぜその形か」まで持たせる

`architecture` は形だけを持っていて、**その形でなければならない理由**が入っていなかった。
膜貫通回数と鎖数は、細胞外の結合をどうやって膜の内側へ渡すかという手段を決めてしまうので、
形と伝達機構は切り離せない。そこを埋めた。

### 設計の修正: architecture をコードからデータへ移した

`validate.py` に語彙をハードコードしていたが、これは**知識**であって語彙ではない。
`data/architectures/registry.yaml` に移し、status と出典を持てるようにした。
`STEP_KINDS` のような純粋な語彙はコードに残す。

各エントリが持つもの:
- `tm` 膜貫通回数 / `chains` 鎖数 / `intrinsic_kinase` 自前のキナーゼの有無
- `crossing` **膜を越えてシグナルを伝える手段**（その形が必然的に要求するもの）
- `why_chains` 鎖を分けることの意味

validate.py の追加チェック:
- `crossing` が空ならエラー（形だけ書いても「なぜその形なのか」が伝わらない）
- `tm: 1` かつ `chains: 1` は WARN（1回貫通・1本鎖では単独で構造変化を伝えられない。
  二量体化するなら chains を2以上に、例外なら crossing に明記する）

### `/architecture` ページを追加

3つの形を並べて「1回貫通は寄らないと伝わらない／7回貫通は1本のねじれで伝わる」を
図で比べられるようにした。経路ページの受容体の段からこのページの該当行へ飛べる。

### 登録した9つの形

| id | 膜貫通 | 鎖 | 膜を越える手段 |
|---|---|---|---|
| `class1_shared` | 1 | 2 | 鎖が寄り、会合した JAK が互いをリン酸化（受容体に酵素活性なし） |
| `class2` | 1 | 2 | 同上（JAK/TYK2） |
| `rtk` | 1 | 2 | 二量体化して自前のキナーゼが自己リン酸化 |
| `stk` | 1 | 2 | II型が I型をリン酸化（向きが決まっている） |
| `tir` | 1 | 2 | 細胞内 TIR が並んで MyD88 の足場になる |
| `tnfr` | 1 | 3 | 三量体リガンドが3本を集め、集合自体がシグナル |
| `sefir` | 1 | 2 | SEFIR が ACT1 を呼ぶ |
| `gpcr` | 7 | 1 | 樽のねじれが G タンパク質を活性化。二量体化が要らない |
| `adipor` | 7 | 1 | 7回貫通だが GPCR ではない。逆向きトポロジーで AMPK 経由 |

`gpcr` `stk` `tir` `tnfr` は既に確認済みの文献で `verified`。残りは `inferred`。

## 2026-08-11（続き11）— 第4のレイヤ: 産生経路

「サイトカインがどの細胞でどういう経路で産生されるか」が構造化されていなかった。
既存の経路8本はすべて**受容側**（外から中へ）で、産生側は0本。
産生・成熟の情報は `form.latency` の散文が15分子ぶんあるだけで、
IL-1β の2シグナル制御も一文のプロズにしかなっていなかった。

### `data/production/` を新設（受容側とは別ディレクトリ）

語彙も向きも違うので混ぜない（通信辺と分化辺を分けたのと同じ理由）。

    受容側 pathways:   ligand → receptor → transducer → tf → output   （外 → 中）
    産生側 production: trigger → transcription → proform → processing → release → extracellular
                                                                      （中 → 外）

`trigger` には `signal` 番号を必須にし、`release` には `route`
（`classical / shedding / gasdermin_pore / cell_death / unconventional`）を持たせた。
validate.py が語彙・付け場所・`release` の存在を検証する。

10分子を登録: IL1B / IL18 / IL1A / IL33 / TNF / TGFB1 / HGF / HAMP / CX3CL1 / IL6。
IL-6 は「制御点が転写1か所しかない」対照として意図的に入れている。

### 「上清が転写量を反映しない」を steps から導出

最初は `is_two_step`（2シグナルか）という真偽値1つにしたが、**判定が誤っていた**。
IL-18 と IL-33 は転写側に引き金が無いだけで放出側にはゲートがあり、
mRNA が上清を予測しない点は IL-1β と同じなのに、拾えていなかった。
また HAMP は signal が2つあるが両方とも転写より前にある「合流」であって2段階のゲートではない。

真偽値をやめ、**4つの理由に分けて導出**するようにした（手で書かず steps から計算）。

| 理由 | 該当 |
|---|---|
| `release_gated` 放出に転写とは別の引き金 | IL-1β, IL-18, IL-1α, IL-33 |
| `post_secretion` 分泌後に細胞外で活性化 | TGF-β1, HGF, IL-18 |
| `membrane_form` 膜型が主体で可溶型は一部 | TNF-α, CX3CL1 |
| `death_release` 細胞死に伴って出る | IL-1α, IL-33 |
| （なし＝転写が律速） | IL-6, ヘプシジン |

原因が違えば対処も違うので、分子ページと `/production` に理由を明示する。

### 図は受容側と逆向きに描く

核を上、細胞質、膜、細胞外を下に置き、上から下へ読むとそのまま産生の順序になる。
`trigger` は左から横に刺し、**2シグナル制御なら矢印が2本、別々の高さで刺さる**。
IL-1β の図でシグナル1が転写へ、シグナル2がカスパーゼ1切断へ入るのが一目で分かる。

### 出典

- PMID:31036962（Swanson, Nat Rev Immunol 2019）— NLRP3 の集合が
  **caspase 1 依存の IL-1β / IL-18 放出**と **gasdermin D によるパイロトーシス**を起こす
- PMID:40450441（Ford, Cytokine Growth Factor Rev 2025）— **IL-1β の分泌は
  通常の ER-Golgi 経路をたどらない**

いずれも abstract に主張そのものが書かれている。他は `inferred`。

## 2026-08-11（続き12）— 公開と、ズレ一覧の死にリンク解消

### GitHub Pages で公開

`ichihara1205/cytokine-atlas` を **public** リポジトリとして作成し、
GitHub Actions で `main` への push からビルドして Pages に配信するようにした。

    https://ichihara1205.github.io/cytokine-atlas/

- ワークフローは `validate.py` を先に走らせる。ERROR があればビルドに進まないので、
  HGNC非承認シンボル・出典なしの verified・不正なプライマー配列は公開されない
- `BASE_URL` 環境変数で base_url を上書きできるようにした
  （プロジェクトページは `/<repo>/` 配下に出るため、Actions が `configure-pages` の
  `base_path` を渡す）
- トップに「記述の大半は未検証」の注意書きを追加。`noindex, nofollow` は維持
- 公開前にコミットのメールアドレスを GitHub の noreply に差し替えた
  （public にすると commit metadata は誰でも読めるため）

Free プランでは private repo から Pages を公開できないため、
リポジトリごと public にする選択をした。`CLAUDE.md` と全 YAML も公開されている。

### 分子5件を追加し、WARN 0 件に

ファミリーの「名前と実体のズレ」に挙げていたのに分子ファイルが無く、
チップが死にリンクになっていた5件を作成した。いずれもズレの主役。

| 分子 | ズレ |
|---|---|
| `IFNL1` (IL-29) | 名前は IL、機能は III型IFN、受容体鎖は IL-10 系 |
| `IFNL2` (IL-28A) | 同上 |
| `GDF2` (BMP9) | GDF と BMP の使い分けに基準がない |
| `MSTN` (GDF8) | GDF 番号で探すと TGF-βスーパーファミリーだと気づけない |
| `IL1F10` (IL-38) | IL-38 は alias。承認シンボルは IL1F10 |

ズレ一覧の分子チップ31件がすべて生きたリンクになり、**死にリンクは0**。
`--strict`（WARN もエラー扱い）も通過するようになった。

BMP-9 は星細胞が出して類洞内皮が受ける（ALK1 が内皮限局）系内完結の軸で、
同じ BMP 枝でも BMP-6（鉄・肝細胞行き）とは受け手も出口も違う。

### 導出が自分の書き間違いを1件捕まえた

MSTN の note に「系内に相手がいない」と書いたのに、`hsc_lx2` を level 1 の受け手に
していたため `system_closure` が `受けるだけ` と判定した。
散文とデータが食い違っていたので note の側を実態に合わせた。
手で `system_closure` を書いていたら気づけなかった類の食い違い。

## 2026-08-11（続き13）— 全文からの出典付け（verified 73 → 97）

### まず内訳を測った

811件の inferred のうち、出典で潰せるのは受容体47・下流46・ファミリー定義46・ズレ29の
**計168件**だけ。producers 191 + receivers 170 = **361件（45%）は原著の図が要る**ので
abstract でも総説の本文でも上げられない。ここを正直に切り分けてから着手した。

### PubMed Central の全文を使った

これまでは abstract しか読んでいなかったが、今回は PMC の全文を取得した。
出典: PubMed。

- Garlanda C, Dinarello CA, Mantovani A. *The interleukin-1 family: back to the future.*
  Immunity 2013;39:1003-18. PMID:24332029 / doi:10.1016/j.immuni.2013.11.010
- Hughes CE, Nibbs RJB. *A guide to chemokines and their receptors.*
  FEBS J 2018;285:2944-2971. PMID:29637711 / doi:10.1111/febs.14466

IL-1 総説の本文には**4つのシグナル受容体複合体が名指しで**書かれており、
abstract では取れなかった分子ごとの受容体対応が確定できた。

| 分子 | 確定した受容体 |
|---|---|
| IL-1α / IL-1β | IL-1R1 + IL-1RAcP |
| IL-33 | ST2 + IL-1RAcP |
| IL-18 | IL-18Rα + IL-18Rβ |

加えて以下も verified に上げた:
TIR ドメイン → MyD88（TLR と共有）／ IL-1α 前駆体は完全活性で壊死により放出・膜型も存在／
IL-1β はカスパーゼ1切断・NLRP3 インフラマソーム依存で、好中球プロテアーゼによる細胞外切断でも活性化／
IL-1Ra は IL-1R1 に高親和性で結合するが IL-1RAcP を呼べない／
IL-18BP は恒常状態で IL-18 の20倍のモル過剰／ IL-33 はカスパーゼ1で**不活性化**される／
IL-33 の供給源に内皮細胞が含まれる／ IL-1β の産生細胞は組織マクロファージ（＝クッパー細胞）。

### 記載の誤りを1件訂正した（IL-38）

IL-38 の受容体を `IL1RL2(IL-36R) / IL1RAPL1` と書いていたが、総説は
**「IL-38 は IL-1R1・IL-1R3・IL-18Rα には結合せず、IL-36R にのみ結合する」**と明記している。
`IL1RAPL1` を削除し、さらに「IL-36R の**部分的**な拮抗剤で、遮断は IL-1Ra より弱い」ことを追記した。

### 全文抽出の欠陥を1件見つけた（記録として）

ケモカイン総説の全文抽出で、**`CX3CL1` が `CXCL1` に、`CX3CR1` が `CXCR1` に化けていた**
（「Two chemokines, CXCL1 and CXCL16, have ... a mucin-like stalk and a transmembrane domain」は
本来 CX3CL1）。そのまま信じると誤った事実を記録するため、**CX3C 系はこの文献からは一切採らなかった。**
名指しが曖昧でない CCL2→CCR2 と CXCL12→CXCR4/ACKR3 のみ verified にしている。

また、ケモカインのリガンド–受容体対応の大半は本文ではなく**図1の中**にあり、
テキストからは読めない。ここが今回の収量が伸びなかった主因。

### 残り

`verified 97 / inferred 787 / todo 4`。
producers/receivers 361件は依然として原著か実測待ちで、abstract でも総説本文でも埋まらない。

## 2026-08-11（続き14）— 図を読むのではなく、本文に書いてある出典を探した

「文献の図を読んで再構成できないのか」という問いへの答え。技術的には可能だが、
**図の読み取りは証拠の質が落ちる**。本文なら「この文が根拠」と引用できるが、
図は目視解釈であり、50ノード超の多対多ネットワーク（ケモカイン総説の図1）で線を1本読み違えれば
`verified` バッジ付きの誤りが混入する。このサイトの価値は verified が信用できることなので割に合わない。

そこで**対応を本文に書いている文献**を探した。

出典: PubMed。Bachelerie F, et al. *International Union of Basic and Clinical Pharmacology.
LXXXIX. Update on the extended family of chemokine receptors and introducing a new nomenclature
for atypical chemokine receptors.* Pharmacol Rev 2013;66:1-79.
PMID:24218476 / doi:10.1124/pr.113.007724

IUPHAR 命名委員会による公式論文で、リガンド–受容体の対応が**本文に明記**されている。
79ページと長いのでローカルに落として該当箇所だけ抽出した。

| 分子 | 確定した受容体 | 本文の根拠 |
|---|---|---|
| CCL2 | CCR2 | 「CCR2 は MCP ファミリー（CCL2/7/13）の高親和性受容体」 |
| CCL5 | CCR1, CCR5 | 「CCR1 は CCL3・CCL5 …に無差別に結合」「CCR5 は CCL2,3,4,5,… を結合」 |
| CXCL8 | CXCR1, CXCR2 | 「CXCL8 誘導の CXCR1/CXCR2 活性化」＋「CXCR2 は7つの ELR+ CXC 全てに結合」「ELR+ の例として CXCL8」 |
| CXCL9 / CXCL10 | CXCR3 | 「CXCR3 は CXCL9・CXCL10・CXCL11 に特異的」＋親和性序列 CXCL11 > CXCL10 > CXCL9 |
| CXCL12 | CXCR4 | 「CXCR4 は CXCL12 の唯一知られた G タンパク質共役型受容体」 |
| CX3CL1 | CX3CR1 | 「CX3CR1 は CX3CL1 の唯一知られたヒト受容体」（monogamous）。US28 にも結合 |

**CCL5 の CCR3 への結合はこの出典では確認できなかった**ため、note に「この出典では確認していない」と
明記したうえで CCR1/CCR5 のみを根拠として verified にした。

`verified 97 → 102 / inferred 787 → 782`。
前回ケモカイン総説（PMID:29637711）で伸びなかったのは、対応が本文ではなく図1にあったため。
正しい打ち手は「図を読む」ではなく「本文に書いてある文献に替える」だった。

## 2026-08-11（続き15）— 第4の status: `figure_read`

出典の質を2種に分けた。**仕様変更なので CLAUDE.md §0.2 の status 表も更新した。**

| status | 意味 |
|---|---|
| `verified` | 一次文献の**本文**または本人の実測で確認済み |
| `figure_read` | 文献の**図**から読み取った。出典と「どの図か」が必須 |
| `inferred` | 一般知識から書いた。未検証 |
| `todo` | 未着手 |

### なぜ分けるか

本文なら「この文が根拠」と引用できるが、図は目視解釈である。
ケモカイン総説の図1のような50ノード超の多対多ネットワークで線を1本読み違えれば、
`verified` バッジ付きの誤りが混入する。同じバッジにすると精度の違いが消える。

`producers` / `receivers` の残り361件は「どの細胞がどれだけ出すか」という定量データで、
その多くは本文の散文ではなく図とグラフにしかない。ここを埋めるときに必要になる。

### 抜け道を塞ぐ仕掛け

- `figure_read` は `source` **必須**（verified と同じ）
- 加えて **`figure` フィールド必須**（例: `figure: "Fig. 1"`）。空ならビルドが止まる。
  どの図を読んだかを残さないと後から検算できず、
  「根拠を示さずに verified を名乗る」抜け道になるため
- `read_note`（何をどう読み取ったか）が無い場合は WARN
- **プライマー配列と抗体は `figure_read` でもサイトに出力しない**（出力は `verified` のみ）。
  配列や品番を図から読み取るのは危険度が高すぎる

壊したデータで検証済み: source 無し・figure 無しの `figure_read` で ERROR 3件が出て
ビルドが停止することを確認した。

### 表示

- バッジ「図から読取」（青枠）。`verified` の通常表示、`inferred` のグレー点線とは別
- `/todo` に専用の節を追加。**どの図を読んだか**を列として出すので、検算対象が一覧できる
- カウンタは `verified 102 / 図から読取 0 / inferred 782 / todo 4`
- トップの注意書きにも「本文に明記された verified より根拠が弱い」と明記

現時点で `figure_read` は**0件**。仕組みだけ用意した状態で、まだ図は読んでいない。
